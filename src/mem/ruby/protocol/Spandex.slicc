protocol "Spandex";

//include "RubySlicc_interfaces.slicc";

include "MOESI_AMD_Base-msg.sm";
include "GPU_VIPER-msg.sm";
include "Spandex-msg.sm";

// 先宣告 Directory，因為 TU / DMA 會用 MachineType:Directory
include "Spandex_LLC.sm";

// 再宣告 TU，因為 TCC 會用 MachineType:TU
include "TU.sm";

// 再宣告 TCC，因為它會送 request 給 TU
include "GPU_VIPER-TCC.sm";

// TCP/SQC 放後面
include "GPU_VIPER-TCP.sm";
include "GPU_VIPER-SQC.sm";

include "Spandex-DMA.sm";
